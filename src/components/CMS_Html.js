import styled from 'styled-components'

const CMS_HTML = styled.div`
  text-align: center;
  max-width: 800px;
  margin: 0 auto;

  h1,
  h2 {
    margin: 20px 0;
  }

  p {
    margin-bottom: 40px;
  }
`

export default CMS_HTML
